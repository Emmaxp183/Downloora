{
    email {$CADDY_EMAIL:admin@example.com}
}

{$APP_DOMAIN:localhost} {
    encode zstd gzip

    handle /__transferd/* {
        reverse_proxy transferd:8080
    }

    handle {
        reverse_proxy app:8000
    }
}
