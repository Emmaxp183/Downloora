apiVersion: v1
kind: Service
metadata:
  name: downloora-app
spec:
  selector:
    app.kubernetes.io/name: downloora-app
    app.kubernetes.io/part-of: downloora
  ports:
    - name: http
      port: 80
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: downloora-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: downloora-app
      app.kubernetes.io/part-of: downloora
  template:
    metadata:
      labels:
        app.kubernetes.io/name: downloora-app
        app.kubernetes.io/part-of: downloora
    spec:
      containers:
        - name: app
          image: downloora-app
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: downloora-config
            - secretRef:
                name: downloora-secrets
          ports:
            - name: http
              containerPort: 8000
          readinessProbe:
            httpGet:
              path: /up
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /up
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
          volumeMounts:
            - name: private-storage
              mountPath: /var/www/html/storage/app/private
            - name: qbittorrent-downloads
              mountPath: /var/www/html/storage/app/private/qbittorrent
      volumes:
        - name: private-storage
          persistentVolumeClaim:
            claimName: downloora-private-storage
        - name: qbittorrent-downloads
          persistentVolumeClaim:
            claimName: qbittorrent-downloads
